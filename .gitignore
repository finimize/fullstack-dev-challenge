# dependencies
/node_modules
client/node_modules

# build
dist

# misc
.DS_Store
npm-debug.log
.idea
.git

# yarn (berry) — commit config + lockfile, ignore cache/state
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
yarn-error.log
